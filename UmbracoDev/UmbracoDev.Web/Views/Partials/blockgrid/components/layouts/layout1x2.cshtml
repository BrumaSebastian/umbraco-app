@inherits UmbracoViewPage<Umbraco.Cms.Core.Models.Blocks.BlockGridItem<Layout1x2, Layout1X2Settings>>

@{
    if (Model?.Areas.Count() != 2) { return; }
    var themeColorsCss = Model.Settings?.GetThemeClasses();
    var layoutStyle = Model.Settings?.GenerateCss<ILayoutProperties>("layout");
    var gridStyles = Model.Settings?.GenerateCss<IGridGapProperties>();
    var areas = Model.Areas.ToList();
}

<div id="layout1x2" class="@themeColorsCss w-full" style="@layoutStyle">
    <div class="grid md:grid-cols-2" style="@gridStyles">
        <div>
            @await Html.GetBlockGridItemsHtmlAsync(areas[0], "itemsSimple")
        </div>
        <div>
            @await Html.GetBlockGridItemsHtmlAsync(areas[1], "itemsSimple")
        </div>
    </div>
</div>